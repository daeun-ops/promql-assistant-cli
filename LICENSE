---
## 패키징 및 빌드 설정

**`pyproject.toml`**
```toml
[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "promql-assistant-cli"
version = "0.1.0"
description = "Natural Language → PromQL CLI Assistant"
readme = "README.md"
requires-python = ">=3.9"
authors = [{ name = "daeun-ops" }]
license = { text = "MIT" }
dependencies = [
  "requests>=2.31.0",
  "typer>=0.12.0",
  "rich>=13.7.0",
]

[project.scripts]
promql-assistant = "promql_assistant_cli.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff]
line-length = 100
select = ["E", "F", "I"]
